<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Casino Online</title>
    <style>
        /* ================= GENERAL ================= */
        * {
          margin: 0;
          padding: 0;
          box-sizing: border-box;
        }

        html, body {
          height: 100%;
        }

        body {
          font-family: Arial, sans-serif;
          background: radial-gradient(circle at center, #121212 0%, #0A0A0A 100%);
          color: #E0E0E0;
          display: flex;
          flex-direction: column; 
          align-items: center;    
          justify-content: flex-start; 
          padding-top: 120px;
        }

        main {
          flex: 1; 
          width: 100%;
          display: flex;
          flex-direction: column;
          align-items: center;
          padding: 20px;
        }

        /* ================= NAVBAR ================= */
        .navbar {
          position: fixed;
          top: 0;
          width: 100%;
          background-color: #1e1e1e;
          border-bottom: 2px solid #C9A227;
          box-shadow: 0 2px 10px rgba(201, 162, 39, 0.2);
          z-index: 1000;
          height: 70px;
          display: flex;
          justify-content: center;
          align-items: center;
          padding: 0 40px;
        }

        .navbar-left {
          position: absolute;
          left: 40px;
          top: 50%;
          transform: translateY(-50%);
        }

        .logo-img {
          height: 120px;
          width: auto;
        }

        .navbar-center {
          display: flex;
          justify-content: center;
          width: 100%;
        }

        .navbar-center ul {
          list-style: none;
          display: flex;
          gap: 40px;
          margin: 0;
          padding: 0;
        }

        .navbar-right {
          position: absolute;
          right: 40px;
          top: 50%;
          transform: translateY(-50%);
        }

        .navbar a {
          text-decoration: none;
          color: #E0E0E0;
          font-weight: bold;
          font-size: 16px;
          transition: color 0.3s ease;
        }

        .navbar a:hover {
          color: #C9A227;
        }

        /* ================= BOTONES ================= */
        .perfil-btn {
          padding: 6px 14px;
          border: 2px solid #C9A227;
          border-radius: 6px;
          background: transparent;
          color: #C9A227;
          font-size: 13px;
          font-weight: bold;
          cursor: pointer;
          transition: all 0.3s ease;
          max-width: 450px;
        }

        .perfil-btn:hover {
          background: #C9A227;
          color: #121212;
          box-shadow: 0 0 8px rgba(201, 162, 39, 0.5);
        }

        .login-btn {
          padding: 12px;
          border: none;
          border-radius: 8px;
          background: #B71C1C;
          color: #E0E0E0;
          font-size: 16px;
          cursor: pointer;
          transition: background 0.3s ease, box-shadow 0.3s ease;
        }

        .login-btn:hover {
          background: #D32F2F;
          box-shadow: 0 0 10px rgba(211, 47, 47, 0.4);
        }

        /* ================= REGISTRO ================= */
        .container {
          margin-top: 50px;
          background: #121212;
          padding: 40px;
          border-radius: 12px;
          box-shadow: 0 20px 40px rgba(201, 162, 39, 0.2);
          border: 2px solid #C9A227;
          max-width: 800px;
          width: 100%;
        }

        .logo h1 {
          color: #C9A227;
          text-align: center;
          margin-bottom: 10px;
        }

        .frase {
          text-align: center;
          margin-bottom: 20px;
          font-style: italic;
        }

        .form-row {
          display: flex;
          flex-wrap: wrap;
          gap: 20px;
        }

        .form-group {
          flex: 1 1 calc(50% - 20px);
          display: flex;
          flex-direction: column;
        }

        .form-group label {
          margin-bottom: 5px;
          font-weight: bold;
          color: #E0E0E0;
        }

        .form-group input {
          padding: 10px;
          border: 1px solid #444;
          border-radius: 6px;
          background: #1e1e1e;
          color: #E0E0E0;
        }

        .submit-btn {
          margin-top: 20px;
          padding: 14px;
          width: 100%;
          background: #B71C1C;
          color: #E0E0E0;
          font-weight: bold;
          border: none;
          border-radius: 8px;
          cursor: pointer;
          transition: background 0.3s ease, box-shadow 0.3s ease;
        }

        .submit-btn:hover {
          background: #D32F2F;
          box-shadow: 0 0 10px rgba(211, 47, 47, 0.4);
        }

        .alt-link {
          margin-top: 15px;
          text-align: center;
        }

        .alt-link a {
          color: #B71C1C;
          text-decoration: none;
          font-weight: bold;
          transition: color 0.3s ease;
        }

        .alt-link a:hover {
          color: #D32F2F;
        }
        /* ================= TRANSACCIÓN ================= */
        .container2 {
          max-width: 500px;
          margin: 40px auto;
          background: #121212;
          padding: 40px;
          border-radius: 12px;
          border: 2px solid #C9A227;
          box-shadow: 0 20px 40px rgba(201, 162, 39, 0.2);
        }

        .titulo-transaccion {
          font-size: 32px;
          color: #C9A227; 
          text-align: center;
          margin-bottom: 12px; 
          text-shadow: 0 0 12px rgba(201,162,39,0.35);
          letter-spacing: 0.5px;
        }

        .container2 .saldo {
          display: block;              
          margin: 25px auto 40px auto; 
          background: rgba(0, 0, 0, 0.6);
          border: 2px solid #C9A227;
          border-radius: 12px;
          padding: 12px 24px;
          text-align: center;
          width: fit-content;        
          font-size: 22px;
          color: #FFEB3B;
          font-weight: bold;
          text-shadow: 0 0 8px rgba(255, 235, 59, 0.8);
          box-shadow: 0 0 15px rgba(201,162,39,0.3);
        }

        .card-transaccion {
          background: #1A1A1A;
          padding: 20px;
          border-radius: 10px;
          margin-bottom: 20px;
          box-shadow: 0 8px 20px rgba(0, 0, 0, 0.4);
          border: 1px solid #444;
        }

        .card-transaccion label {
          font-size: 14px;
          font-weight: bold;
          color: #E0E0E0;
          margin-bottom: 6px;
          display: block;
        }

        .card-transaccion input {
          width: 100%;
          padding: 10px;
          font-size: 14px;
          border: 1px solid #444;
          border-radius: 6px;
          background: #1e1e1e;
          color: #E0E0E0;
          margin-bottom: 14px;
        }

        .card-transaccion button {
          width: 100%;
          padding: 14px;
          font-size: 16px;
          font-weight: bold;
          border-radius: 8px;
          background: #B71C1C;
          color: #E0E0E0;
          border: none;
          cursor: pointer;
          transition: background 0.3s ease, box-shadow 0.3s ease;
        }

        .card-transaccion button:hover {
          background: #D32F2F;
          box-shadow: 0 0 10px rgba(211, 47, 47, 0.4);
        }

        /* ================= PERFIL ================= */
        .box-perfil-container {
          text-align: center;
          background: #121212;
          padding: 20px;
          border-radius: 12px;
          box-shadow: 0 15px 30px rgba(201, 162, 39, 0.2);
          position: relative;
          overflow: hidden;
          width: 90%;
          border: 2px solid #C9A227;
          max-width: 500px; 
          margin: 0 auto;
        }

        .perfil-header {
          text-align: center;
          margin-bottom: 20px;
        }

        .perfil-avatar {
          width: 90px;
          height: 90px;
          border-radius: 50%;
          border: 3px solid #C9A227;
          margin-bottom: 10px;
        }

        .perfil-header h1 {
          font-size: 24px;
          color: #C9A227;
          text-shadow: 0 0 8px rgba(201, 162, 39, 0.6);
        }

        .correo {
          color: #B0BEC5;
          margin-top: 5px;
          font-size: 14px;
        }

        .perfil-info,
        .perfil-saldo,
        .perfil-transacciones {
          margin-bottom: 20px;
          text-align: left;
        }

        h2 {
          color: #C9A227;
          margin-bottom: 8px;
          border-bottom: 1px solid #444;
          padding-bottom: 4px;
          font-size: 18px;
        }

        .saldo {
          font-size: 1.3rem;
          font-weight: bold;
          color: #4CAF50;
        }

        table {
          width: 100%;
          border-collapse: collapse;
          margin-top: 8px;
          font-size: 14px;
        }

        table th,
        table td {
          padding: 8px;
          text-align: center;
        }

        table th {
          background: #C9A227;
          color: black;
        }

        table tr:nth-child(even) {
          background: #2c2c2c;
        }
        table tr:nth-child(odd) {
          background: #1a1a1a;
        }

        .perfil-actions {
          text-align: center;
          margin-top: 15px;
        }

        .btn-ruleta {
          display: inline-block;
          padding: 10px 20px;
          background: #B71C1C;
          color: #E0E0E0;
          font-weight: bold;
          text-decoration: none;
          border-radius: 8px;
          transition: 0.3s ease;
          font-size: 16px;
        }

        .btn-ruleta:hover {
          background: #D32F2F;
          box-shadow: 0 0 10px rgba(211, 47, 47, 0.5);
          transform: scale(1.05);
        }

        /* ================= LOGIN FORM ================= */
        .login-form {
          background: #121212;
          padding: 40px;
          border-radius: 12px;
          box-shadow: 0 20px 40px rgba(201, 162, 39, 0.2);
          border: 2px solid #C9A227;
          max-width: 500px;
          width: 100%;
          margin-top: 100px;
        }

        .login-title {
          color: #C9A227;
          text-align: center;
          margin-bottom: 20px;
        }

        .login-form-group {
          margin-bottom: 20px;
        }

        .login-form-group label {
          font-weight: bold;
          margin-bottom: 8px;
          display: block;
          color: #E0E0E0;
        }

        .login-input {
          padding: 12px;
          border: 1px solid #444;
          border-radius: 6px;
          background: #1e1e1e;
          color: #E0E0E0;
          width: 100%;
        }

        .login-submit-btn {
          background: #B71C1C;
          color: #E0E0E0;
          font-weight: bold;
          border: none;
          padding: 14px;
          border-radius: 8px;
          cursor: pointer;
          transition: background 0.3s ease, box-shadow 0.3s ease;
          width: 100%;
        }

        .login-submit-btn:hover {
          background: #D32F2F;
          box-shadow: 0 0 10px rgba(211, 47, 47, 0.4);
        }

        .login-alt-link,
        .login-forgot {
          text-align: center;
          margin-top: 15px;
          display: block;
          font-weight: bold;
        }

        .login-alt-link a,
        .login-forgot {
          color: #B71C1C;
          text-decoration: none;
          transition: color 0.3s ease;
        }

        .login-alt-link a:hover,
        .login-forgot:hover {
          color: #D32F2F;
        }
        
        /* ================= TÍTULOS DE PÁGINA ================= */
        main h1 {
          text-align: center;
          color: #C9A227; 
          text-shadow: 0 0 12px rgba(201,162,39,0.35);
          letter-spacing: 0.5px;
          font-size: clamp(28px, 4vw, 40px);
          margin-bottom: 12px;
        }

        /* ================= SECCIONES (ABOUT) ================= */
        .about,
        .team,
        .tech {
          background: #121212; 
          border: 1px solid rgba(255,255,255,0.06);
          border-radius: 12px; 
          padding: 32px;
          box-shadow: 0 14px 30px rgba(201, 162, 39, 0.25); 
          max-width: 1100px; 
          width: 100%;
          margin: 0 auto;
        }

        .about + .team,
        .team + .tech {
          margin-top: 24px; 
        }

        .about::before,
        .team::before,
        .tech::before {
          content: "";
          display: block;
          height: 3px;
          margin: -32px -32px 24px;
          background: linear-gradient(90deg, #C9A227, transparent 70%);
          border-top-left-radius: 12px;
          border-top-right-radius: 12px;
        }
        .about h2,
        .team h2,
        .tech h2 {
          color: #E0E0E0; 
          font-size: clamp(20px, 2.5vw, 28px);
          border-left: 4px solid #C9A227; 
          padding-left: 12px;
          margin-bottom: 16px;
          border-bottom: none !important;
          padding-bottom: 0 !important;
        }

        .about p {
          color: #B0BEC5; 
          font-size: 16px;
          max-width: 80ch;
        }

        .team > div {
          display: grid;
          grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
          gap: 24px; 
          align-items: start;
        }
        .team > div > h2 {
          grid-column: 1 / -1;
        }

        .card {
          background: linear-gradient(180deg, rgba(255,255,255,0.03), rgba(255,255,255,0.01));
          border: 1px solid rgba(255,255,255,0.06);
          border-radius: 12px;
          padding: 20px;
          text-align: center;
          transition: transform 0.25s ease, box-shadow 0.25s ease, border-color 0.25s ease;
        }
        .card:hover {
          transform: translateY(-4px);
          box-shadow: 0 16px 32px rgba(201, 162, 39, 0.25);
          border-color: #C9A227;
        }
        .card img {
          width: 120px;
          height: 120px;
          object-fit: cover;
          border-radius: 50%;
          border: 2px solid #C9A227;
          box-shadow: 0 0 10px rgba(201,162,39,0.35);
        }
        .card h3 {
          font-size: 18px;
          color: #E0E0E0;
          margin-top: 10px;
        }
        .card p {
          color: #B0BEC5;
          font-size: 14px;
          margin-top: 8px;
        }
        .card p strong {
          color: #E0E0E0;
        }

        .tech-grid {
          list-style: none;
          display: grid;
          grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
          gap: 16px;
          margin-top: 16px;
          padding: 0;
        }
        .tech-item {
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          gap: 8px;
          background: rgba(255,255,255,0.03);
          border: 1px solid rgba(255,255,255,0.06);
          border-radius: 12px;
          padding: 16px;
          transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
        }
        .tech-item:hover {
          transform: translateY(-3px);
          border-color: #C9A227;
          box-shadow: 0 10px 20px rgba(201, 162, 39, 0.25);
        }
        .tech-item img {
          width: 56px;
          height: 56px;
          object-fit: contain;
          filter: drop-shadow(0 2px 6px rgba(0,0,0,0.3));
        }

        /* ================= HOME PAGE ================= */
        .casino-home-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 1rem;
        }
        .casino-home-hero {
            padding: 5rem 1rem;
            text-align: center;
        }

        .casino-home-hero h1 {
            font-size: 3rem;
            font-weight: bold;
            margin-bottom: 1.5rem;
            color: #C9A227;
            text-balance: balance;
        }

        .casino-home-hero-divider {
            width: 6rem;
            height: 4px;
            background-color: #C9A227;
            margin: 0 auto 2rem auto;
        }

        .casino-home-hero p {
            font-size: 1.25rem;
            margin-bottom: 3rem;
            max-width: 48rem;
            margin-left: auto;
            margin-right: auto;
            line-height: 1.6;
            color: #E0E0E0;
        }

        .casino-home-cta-buttons {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            align-items: center;
            justify-content: center;
        }

        .casino-home-btn {
            padding: 1rem 2rem;
            font-size: 1.125rem;
            font-weight: 600;
            text-decoration: none;
            border-radius: 0.5rem;
            transition: all 0.3s ease;
            display: inline-block;
            border: 2px solid transparent;
        }

        .casino-home-btn-primary {
            background-color: #B71C1C;
            color: #E0E0E0;
        }

        .casino-home-btn-primary:hover {
            background-color: #D32F2F;
        }

        .casino-home-btn-outline {
            background-color: transparent;
            color: #C9A227;
            border-color: #C9A227;
        }

        .casino-home-btn-outline:hover {
            background-color: #C9A227;
            color: #121212;
        }
        .casino-home-nav-section {
            padding: 4rem 1rem;
        }

        .casino-home-cards-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 1.5rem;
        }

        .casino-home-card {
            background-color: #1A1A1A;
            border: 2px solid #C9A227;
            border-radius: 0.5rem;
            padding: 1.5rem;
            text-align: center;
            text-decoration: none;
            transition: transform 0.3s ease;
            display: block;
            color: inherit;
        }

        .casino-home-card:hover {
            transform: scale(1.05);
        }

        .casino-home-card-icon {
            width: 4rem;
            height: 4rem;
            background-color: #B71C1C;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1rem auto;
            font-size: 1.5rem;
        }

        .casino-home-card h3 {
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
            color: #C9A227;
        }

        .casino-home-card p {
            color: #B0BEC5;
        }
        .casino-home-about-section {
            padding: 4rem 1rem;
        }

        .casino-home-about-card {
            background-color: #1A1A1A;
            border: 2px solid #C9A227;
            border-radius: 0.5rem;
            padding: 2rem;
            max-width: 64rem;
            margin: 0 auto;
        }

        .casino-home-about-header {
            display: flex;
            align-items: center;
            margin-bottom: 1.5rem;
        }

        .casino-home-about-divider {
            width: 4px;
            height: 3rem;
            background-color: #C9A227;
            margin-right: 1rem;
        }

        .casino-home-about-header h2 {
            font-size: 1.875rem;
            font-weight: bold;
            color: #E0E0E0;
        }

        .casino-home-about-text {
            font-size: 1.125rem;
            line-height: 1.6;
            color: #B0BEC5;
        }

        /* ================= RULETA ================= */
        .ruleta-header {
            display: flex;
            justify-content: center;
            align-items: flex-start;
            padding: 3rem 1rem;
            gap: 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .area-ruleta {
            flex: 2;
            background-color: #1A1A1A;
            border: 2px solid #C9A227;
            border-radius: 0.75rem;
            padding: 2rem;
            text-align: center;
        }

        .titulo-ruleta {
            font-size: 2rem;
            font-weight: bold;
            color: #C9A227;
            margin-bottom: 1.5rem;
        }

        .ruleta-image {
            max-width: 100%;
            border-radius: 0.5rem;
        }

        .sidebar {
            flex: 1;
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        .ruleta-info-card {
            background-color: #1A1A1A;
            border: 2px solid #C9A227;
            border-radius: 0.5rem;
            padding: 1.25rem;
        }

        .titulo-info-ruleta {
            font-size: 1.25rem;
            font-weight: bold;
            color: #C9A227;
            margin-bottom: 0.75rem;
        }

        .status-ruleta {
            font-size: 1.1rem;
            color: #E0E0E0;
        }

        .casino-ruleta-numbers-grid {
            display: flex;
        }

        footer {
          margin-top: auto; 
        }

    </style>
</head>
<body>
    <nav class="navbar">
        <div class="navbar-left">
            <img src="/images/logocasino.png" alt="Logo del Casino" class="logo-img">
        </div>
        <div class="navbar-center">
            <ul>
                <li><a href="/home">Home</a></li>
                <li><a href="/casino">Casino</a></li>
                <li><a href="/rules">Reglas</a></li>
                <li><a href="/transaccion">Depósito</a></li>
                <li><a href="/about">Nosotros</a></li>
                <li><a href="/perfil">Perfil</a></li>
            </ul>
        </div>
        <div class="navbar-right">
            {{#if user}}
                <a href="/perfil" class="perfil-btn">{{user.email}}</a>
                <a href="/logout" class="login-btn">Cerrar sesión</a>
            {{else}}
                <a href="/register" class="perfil-btn">Registrarse</a>
                <a href="/login" class="login-btn">Iniciar sesión</a>
            {{/if}}
        </div>
    </nav>

    <main>
        {{{body}}}
    </main>

    <footer>
      <p>&copy; 2025 Casino Online. Todos los derechos reservados.</p>
    </footer>
</body>
</html>

